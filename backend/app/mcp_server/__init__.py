"""MCP Server — exposes the agent system's capabilities as MCP tools.

External clients (Claude Desktop, Cursor, custom scripts) can connect
via stdio or SSE and query databases, search knowledge, manage projects.
"""

"""Analytics vendor sources (GA4, App Store Connect, Google Play).

Collection primitives shared by every vendor adapter live here: the error
taxonomy (:mod:`app.analytics.errors`), the injectable retrying transport
(:mod:`app.analytics.http`) and the run outcome (:mod:`app.analytics.outcome`).
"""

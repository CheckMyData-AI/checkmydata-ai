# T01 · P0 · ops — Верифицировать волну часа 0 и закрыть двухдневный разрыв

**Цель:** доказать, что фикс #312 работает на проде, и вернуть esim-php свежесть.
**Кода не пишет.** Оценка: 20 минут + одна ночь ожидания.

## Контекст

- Дефект: диспетчер волны перечитывал часы после сна; за 8 часов подряд волна отработала
  за часы `20,21,21,23,23,1,2,3` — час 0 (единственный назначенный) пропущен дважды.
  Receipts: лог web-дино 2026-09-07T18:00…09-08T01:00; `indexing_runs`: последний
  `daily_sync` = 2026-09-06 22:00 UTC.
- Фикс: #312 (`_dispatch_daily_knowledge_sync_wave(at=…)`, циклы передают `at=next_hour`),
  смержен 2026-09-08 ~11:50 CEST; деплой = релиз **после v345**.
- Час 0 Europe/Berlin = **22:00 UTC** сейчас (DST). Расписание проекта — глобальный
  дефолт (`sync_schedule_enabled/hour = NULL` у всех 4 проектов — проверено).

## Шаги

1. **Деплой дошёл:** `heroku releases -a checkmydata-api -n 3` — есть релиз новее v345 с
   коммитом `7b1f7402`. В boot-логе нет новых ошибок; строка
   `Plan grant reconcile at startup: ok (granted=0 unchanged=2 …)` на месте.
2. **Догон вручную (не ждать ночи):** `POST /api/projects/{id}/sync-now` за владельца
   esim-php (или кнопка Sync now в UI). Ожидание: `indexing_runs` получает `daily_sync`
   (trigger=manual) → `completed`; инкрементальный, ориентир 90–2 000 с
   (avg completed 30д = 1 896 с).
3. **Ночная верификация (следующее утро):** в логе за 22:00 UTC строка
   `Cron: daily knowledge sync wave dispatched projects=1 skipped=3 unentitled=0 hour=0`
   — решает `hour=0` и `projects=1`. В `indexing_runs` — свежий `daily_sync`
   `trigger=schedule` со статусом `completed`.
4. **Отрицательный контроль:** в последовательности часов за сутки каждый час встречается
   ровно один раз (`heroku logs | grep "sync wave dispatched"`).

## Definition of Done

- [ ] Шаги 1–4 с выводами команд вставлены в комментарий к строке T01 в PR-леджере.
- [ ] Если шаг 3 покажет НЕ `hour=0` — задача переоткрывает #312-разбор, статус
  `blocked(hour-mismatch)`, приложить полную последовательность часов.
- [ ] Леджер PLAN.md обновлён.

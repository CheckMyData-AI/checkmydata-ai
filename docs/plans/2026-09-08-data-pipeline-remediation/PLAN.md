# Data-pipeline remediation — master plan

> Источник: `docs/reports/data-pipeline-audit-2026-09-08.html` (+ `.json` сайдкар с сырыми
> выборками). Каждая задача этого плана — строка борда из аудита, проработанная до пакета,
> который исполняющий агент может взять без доступа к этой сессии. Контракт: **пакет несёт
> receipts (`file:line`, измеренные числа), TDD-порядок, наблюдаемость, гейты и rollback**.

## Цель

Конвейер данных (repo-индексация, DB-индексация, code↔DB-карта, эмбеддинги, расписание)
становится: (1) **честным** — карта не содержит фантомов, статусы не противоречат сами
себе; (2) **дешёвым** — force_full не платит LLM за неизменившиеся документы; (3)
**наблюдаемым** — деградация и приближение к потолкам видны до отказа; (4)
**доказуемо эксклюзивным** — двойной прогон невозможен между процессами.

## Правила исполнения (обязательны для каждого пакета)

1. **TDD**: тесты из секции «Тесты» пакета пишутся первыми и должны упасть до реализации.
2. **Гейты** — локально до PR: см. `CONVENTIONS.md` § Гейты. Без зелёного — не done.
3. **Наблюдаемость — часть задачи, не follow-up**: логи/счётчики из секции пакета входят
   в тот же PR.
4. **Документы в том же изменении**: `CLAUDE.md`/`API.md`/`docs/ux/scenarios.md` — если
   пакет их называет. Плюс строка в леджере ниже.
5. **Один PR на задачу** (T-xx), squash-merge, заголовок `<type>(scope): …` + `(T-xx)` в
   теле. Ветка `feat/T0x-slug` или `fix/T0x-slug`.
6. Ловушки репозитория, о которые уже спотыкались, собраны в `CONVENTIONS.md` § Ловушки —
   читать до первого коммита.

## Леджер прогресса

**Правило обновления**: PR задачи меняет её строку в этом файле в том же PR
(status → `in-progress` при старте ветки, `done` + PR# + одна строка доказательства при
мерже; `blocked(причина)` — если упёрлись). Никаких отдельных «статусных» коммитов.

| ID | Задача | Pri | Статус | PR | Доказательство |
|----|--------|-----|--------|----|----------------|
| T01 | Верифицировать волну часа 0 и закрыть двухдневный разрыв | P0 | done | — | **Догон прошёл 09-08 21:19→22:52** (index_repo 3714 с → db_index 1634 с → code_db_sync 231 с). Граф 25 897 симв. / 64 908 рёбер, **IMPORTS 16 442** (было 0). Карта свежая 22:52:31 после двух суток. **Фантомов 0** (было 43, из них 20 `db_only`) — гард T02 подтверждён на проде. Часы монотонны. Остаётся наблюдение волны часа 0 || T02 | Целостность карты code↔DB: фантомы невозможны | P1 | done | #314 | механизм найден по данным; резолвер + гард + удаление отказанных + evidence-гейт; 8064 теста; **прод 09-09: 0 фантомов из 308 строк** |
| T02 | Целостность карты code↔DB: фантомы невозможны | P1 | merged | #314 | механизм найден по данным; резолвер + гард + удаление отказанных + evidence-гейт. **Прод 09-09: 0 фантомов из 308 строк** (было 43, из них 20 `db_only`) |
| T03 | generate_docs: кэш по content_hash + модель per doc_type | P1 | merged | #317 | кэш по входам документа + `should_reuse_document`; `existing_docs_map` грузился ТОЛЬКО на инкременте — дорогой путь сравнивать было не с чем; `DOC_GEN_SCHEMA` вне `embedding_fingerprint` в обе стороны. Прод-цифра ждёт деплоя; **прод-цифра получена 09-09: полный ребилд 5 609 с** против 12 039–12 329 с холодного, `generate_docs` 2 687 с против 9 375 с (**−71%**), `generated=188 reused=575`, все 763 дока с хэшем. Кэш рос между прерванными попытками: 304 → 575 |
| T04 | db_index `trigger=auto`: диагностика и потолок fetch_samples | P1 | merged | #319 | T04.0: реконсайлер на проде ВЫКЛЮЧЕН (`freshness_reconciler_enabled` не задан) → все `auto`-прогоны, включая 8ч25м, запускал ЧЕЛОВЕК кнопкой test/refresh-schema. Бюджет 1800 с, пропуск по именам, сводка всегда. Прод-цифра ждёт следующего auto-прогона |
| T05 | EMBEDDING_UPSERT_BATCH_SIZE: измерить 32/64 на Standard-2X | P2 | done | — | **Измерено 2026-09-09: 32 НЕ помещается в 1 ГиБ.** v351 → `code_symbol_embed` на 26 014 символах → R14 через 20 с, 1082→1094 МБ (105.7→106.9%), рост. Откат v352, пересборка переставлена, `config-drift` зелёный. 16 не проверялось: каждая проба стоит полного ребилда единственного прод-проекта |
| T06 | Алерт приближения к потолку daily_sync (7215 с из 7200 уже было) | P2 | merged | #320 | `budget_warning` на 85% бюджета + counter; доля, а не вторая константа — иначе поднятие потолка делает алерт вечным |
| T07 | Кросс-процессная эксклюзивность repo-index: доказать и понизить dict | P2 | merged | #320 | «Still open» оказалось неверным: `IndexingRun` создаётся в ОДНОМ месте, частичный уникальный индекс есть и на SQLite, и на PG; Redis-лок не нужен |
| T08 | Enforcement max_index_bytes (вариант «а») | P2 | merged | #322 | предупреждение, не блокировка; протокол НЕ расширен — предупреждение не спрашивает разрешения, число читается модульным хелпером `index_quota_bytes` с деградацией в «безлимит». SCN-152 |
| T09 | token_usage: строки с cost>0 при tokens=0 | P3 | merged | #321 | механизм: августовский фикс лёг только в router, четыре запросных писателя в `chat.py` шлют `.get(...,0)`; деривация перенесена в воронку `record_usage`. 10 строк из 9423, backfill непропорционален |
| T10 | **Ночная индексация рвётся: реап убивает живой ран** | P0 | done | — | гипотеза «глушит запись» **измерением опровергнута**: глушит *построение* графа — `len(G.edges)` на каждое ребро, O(E×(V+E)), 209.6 с → 0.10 с. T10.1 (#315) дал бит, T10.2 — дельта вместо 94k строк, T10.4 — квадратичность |
| T11 | Reindex удаляет векторы и не подтверждает постановку пересборки | P0 | merged | #324 | найдено 2026-09-09 при исполнении T05, на живом проде: коллекция удалена, `job=None` залогирован как успех, маркер `embedding_fingerprint` сдвинут. Ночной крон это не чинит — он `force_full=False` |
| T12 | `code_symbol_embed` терял heartbeat — **опровергнуто, дефекта нет** | P0 | done | #327 | три случая, механизм один: процесс забрали. 23:44→v352 в 23:47:54; 23:54→v353 в 00:00:45; 00:29 → `SIGTERM` в 00:49:59 **без деплоя** (ротация дино Heroku). Временный порог 3600 снят (v357) — он же блокировал замену два часа через `2 × timeout`. Вырос в T13 |
| T13 | Рестарт процесса — не сбойный прогон; воркер возвращает то, что заменил | P0 | merged | #328 | следствие T12: раны убивают деплои **и штатная ротация дино**. Реапер ждал 300 с и тратил бюджет для настоящих сбоев — 01:01:26 отказал вживую. Теперь `boot_id`+`owner` на прогоне и зачистка при старте воркера; `HEROKU_RELEASE_VERSION` ротацию не ловит, `BOOT_ID` ловит; **прод 09-09 07:27:41: зачистка сработала** — деплой перезапустил воркер на `generate_docs`, заменивший воркер вернул ран (`force_full=True`, job выдан), `1 put back`, замена стартовала через 36 с |
| T14 | Пул Supavisor исчерпан: приложение просит до 34 при потолке 15 | P0 | merged | #331 | арифметика жила в трёх файлах, не знавших друг о друге. Теперь `DB_CONNECTION_CEILING`: при заданном потолке boot **отказывается** от конфигурации, которая не влезает — `(pool+overflow+vector)*2+1 <= ceiling`, где 2 = web+worker, а +1 зарезервирован человеку (насыщение нашлось именно упавшим `psql`). Дефолт `0` ничего не проверяет: у self-hosted потолка нет. Поднятие самого потолка требует `supabase login` — единственный шаг для человека; **закрыто 09-09 полностью**: `default_pool_size` 15 → **40** через Management API, `DB_CONNECTION_CEILING=40` (v365). Худший случай 35 ≤ 40; у базы остаётся 17 из 57 (`max_connections` 60 − 3) на прямые подключения. Проверено: `psql`, который отбивался с `EMAXCONNSESSION`, подключился с первой попытки |
| T15 | Воркер вообще не мог ничего поставить в очередь | P0 | merged | #330 | найдено 2026-09-09 **ERROR-строкой, которую T13 для этого и завёл**: `init_task_queue` вызывался только в lifespan web, поэтому `_arq_pool` в воркере всегда `None`. Ломало не только зачистку T13, но и `StaleRunReaper._requeue` из воркерского `reaper_loop` — то есть бюджет реапера выглядел исчерпанным, а на деле постановка не работала |

## Волны и зависимости

```
Волна A (сейчас):             T10 → T01   (T01 не пройдёт, пока прогон реапится)
Волна B (параллельно):        T02   T04   T06   T09
Волна C (после B по файлам):  T03 → T08(после решения D5; оба трогают pipeline_runner)
Независимо (конфиг+замер):    T05   T07
```

Зависимости содержательные: T08 читает счётчик, который T03 не меняет, но **оба правят
`pipeline_runner.py`** — не вести параллельно. T05 лучше ПОСЛЕ T03: пик памяти шага
embed меняется от батча, а не от кэша доков, но замер чище на стабильном пайплайне.

## Матрица файлов (для параллельных агентов; agent-sync в проекте не настроен —
двум агентам в одном файле не работать)

| Файл | T02 | T03 | T04 | T06 | T07 | T08 |
|---|---|---|---|---|---|---|
| `app/knowledge/code_db_sync_pipeline.py` | ✦ | | | | | |
| `app/knowledge/repo_analyzer.py` | ✦ | | | | | |
| `app/knowledge/pipeline_runner.py` | | ✦ | | | | ✦ |
| `app/knowledge/db_index_pipeline.py` | | | ✦ | | | |
| `app/worker.py` / `daily_knowledge_sync_service.py` | | | | ✦ | | |
| `app/api/routes/repos.py`, `run_coordinator.py` (тесты) | | | | | ✦ | |
| `app/entitlements/*`, `plan_catalogue.py` | | | | | | ✦ |

## Открытые решения (нужен оператор)

| # | Решение | Блокирует |
|---|---------|-----------|
| ~~D5~~ | **Решено 2026-09-08: вариант (а)** — warning + строка в Needs-you, без блокировки. В духе остального биллинг-шва: деградация открытая и названная, а не молчаливая остановка. | T08 разблокирован |

## Верификация плана в целом

- После T02: SQL из пакета возвращает `phantom_rows = 0` на проде после ближайшего sync.
- После T03: следующий force_full < 4 500 с (сейчас 12 039–12 329 с) при
  `docs_reused_total ≥ 700`.
- После T04: следующий `trigger=auto` db_index < 1 800 с или его отсутствие объяснено.
- Все: `make check` зелёный, леджер выше без строк `in-progress` старше недели.

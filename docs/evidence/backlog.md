# Board

Rows carry a source and a computed priority. The 2026-09-13 audit's fourteen projects are
sequenced by ADR-0005 (waves), ADR-0006 (buy-before-build) and ADR-0007 (recipes, shell);
this board holds what falls outside a named project or arrives mid-run.

| id | Priority | Row | Source | Status |
|---|---|---|---|---|
| B-01 | P1 | **Closed — the guard was already codebase-wide.** `tests/unit/llm/test_no_raw_tool_argument_reaches_a_typed_sink.py` walks `APP.rglob("*.py")`, not one analyzer: it flags a type-assuming method, builtin or slice applied to a raw tool argument anywhere in `app/`, exempts lines inside a `TypeError`/`ValueError`/`AttributeError` handler, and carries a reason for every allowlist entry. This row described a narrower state than the code was in. | verified 2026-09-15 by reading the walker | **closed 2026-09-15** |
| B-02 | P1 | **Half done, and the measurement changed the shape.** The unit suite is the wrong target — 128 of its files build their own in-memory engine deliberately, for speed; the integration suite is what writes through the ORM. `TEST_DATABASE_URL` now selects the engine there (SQLite default, 688 tests unchanged). Run against `pgvector/pgvector:pg17` it found four differences: the `vector` extension is needed for `doc_embeddings`; asyncpg refuses multi-statement DDL; **the harness holds an uncommitted transaction so every second connection blocks** (`_grant_project_creation` idle-in-transaction on `users` vs the request path's `email_verified` update waiting on `Lock`/`transactionid` — 198 s on SQLite, unfinished at 33 min); and **PostgreSQL aborts a transaction after a failed statement** where SQLite continues, so code that swallows a DB error and reuses the session is already broken in production and green in CI. **CI job deliberately not added** — a job failing on hundreds of harness errors teaches people to ignore it. Next step: make the harness commit instead of holding one open transaction. | measured 2026-09-15 | open — next step named |
| B-03 | P1 | **Closed, and the premise was half wrong.** The row assumed several modules compose psycopg SQL; measured, **two** do — `PgVectorStore` (covered by the runtime test) and `models/base.py`, whose `pg_advisory_lock` f-string was uncovered and is safe only because it interpolates an integer constant. `test_every_psycopg_statement_compiles.py` is a static reader: it resolves each statement (literal, f-string literal parts, concatenation), neutralises interpolations and runs psycopg's own client-side scanner — which reads the LITERAL text, so a stray `%` survives every interpolation. Verified by planting one in the advisory-lock statement. A companion test fails if the walker stops finding both composers by name. | measured 2026-09-15 | **closed 2026-09-15** |
| B-04 | P2 | `_wf_*` dicts → per-request `RequestState`; PRJ-01 made the `extra` dict the single carrier, the drains still exist beside it | PRJ-01 carry-over → PRJ-03 | open |
| B-06 | **P0** | **The map's collapse was a completion cap, not a model — and the cause recorded here was wrong.** This row said `deepseek/deepseek-v4-flash-0731` "frequently returns no tool call at all". It returns one every time; the call is cut off mid-argument. Measured 2026-09-15 against fourteen real production tables: **all fourteen stopped at exactly the 2048-token cap**, the arguments JSON never closed, the adapter substituted `args = {}` and appended the call anyway, and `_analysis_from_args({})` wrote a full row of defaults with `is_fallback=False` — which is precisely what blinded `sync_min_success_ratio_to_persist`, the guard built to stop a degraded model overwriting a good map. Production damage: 126 of 263 rows `unknown`, 129 with no column notes, 121 at default confidence. `finish_reason` carried the truth from all three adapters and **no production path read it**. Fixed: truncation refused at six call sites, `sync_status` moved to second in the schema, caps became settings, AST guard verified against a planted defect. Measured through the shipped path on the current model: **0/14 decided → 13/14**. | PRJ-01 post-deploy measurement; probe over 14 production tables | **closed 2026-09-15** |
| B-08 | **P0** | **Closed, and the cause was ours rather than the model's.** The index MEASURED `purchases.currency` at `distinct_count: 14` (BRL…VND) and stored it in `column_stats_json` — then `_build_table_prompt` rendered the column as `name: type` and showed the model neither the count nor the values, so the note it wrote read "likely USD" and the hint said to divide `amount` by 100 for dollars. Measurement, claim and consumer in one row, nothing comparing them. Fixed in two layers: the prompt now renders `[measured: 14 distinct; values: …]` (silence where nothing was measured), and `apply_measured_corrections` prepends the counted fact to `query_hints` when a money column sits beside a multi-valued currency column — additive, and only where a measurement exists. AST guard over both `TableAnalysis` build sites, verified by planting a bypass. **Takes effect on the next `db_index` run.** | measured 2026-09-15 | **closed 2026-09-15** |
| B-09 | **P0** | **Closed by computing the relationship the documents cannot carry.** `payment_histories` sat at relevance 4, accurately described and recommended, while being 4.3x off `purchases` for one month and a different factor every other. `BaseConnector.period_total` now runs the same `SUM` over the last complete month on both, and `rival_tables` stores the divergence in front of the generated hints. Narrow by construction: money column plus date column, no tallies or ratios, no float money, no views, no unindexed table over 2M rows, `created_at` preferred; six candidates, eight pairs, one aggregate per table, wall-clock bounded. Degrades to silence — a failed query, an empty period on either side, or a spent budget yields no fact, because a fabricated warning is worse than none. 28 tests. **Takes effect on the next `db_index` run.** | measured 2026-09-15 | **closed 2026-09-15** |
| B-10 | P1 | **A generated non-answer is not stored now.** 25 of 782 documents said in prose the file had nothing to do with the database (all three `query_pattern` documents among them), 16 more described build output and vendor code, and two `.gitkeep` files were stored as migration documents because that branch tested the path and never the content. Fixed in two places: `can_carry_schema()` refuses the file before extraction by shape — unauthored directory, placeholder, too small, or a line over 2 000 chars, which is what catches `vendor.js` rather than its name — and `is_non_answer()` refuses the document after generation for the file that looked plausible. Both storage sites checked by an AST guard; `generate_docs` reports `non_answers=`. | audit §2; production corpus 2026-09-15 | **closed 2026-09-15** |
| B-11 | P1 | **Closed by making each document state its own kind.** Re-measured 2026-09-15: 537 of 782 documents are migrations, of which **286 `alter` and 13 `drop`** — each describing an intermediate table state that a later migration superseded, competing in retrieval with the current schema on equal terms. The agent's only clue was the file path. A retrieved chunk now carries a one-line qualifier: `migration` is HISTORY and `orm_model` is CODE, each naming the database index as authoritative rather than merely casting doubt; every other type carries none, since marking everything is marking nothing. Reorganising the corpus by subject stays open as design work (`docs/design/2026-09-14-…`); this closes the part that was producing wrong answers. 11 tests, guard verified against a planted defect after its first version proved too loose. | measured 2026-09-15 | **closed 2026-09-15** |
| B-12 | P2 | **Closed — the measurement is labelled now.** Re-measured 2026-09-15: **139 of 214** tables carry hedged prose and **209 of 214** carry real column statistics, so the row's 65% is current. The row's other half was stale: `purchases` does have stats. The live defect was one layer later than B-08 — `schema_context_builder` rendered `column_distinct_values_json` and **never** `column_stats_json`, so the agent saw value lists the sampler had spent on identifiers and not the counts. Now a labelled `MEASURED (counted by the indexer, not inferred)` block leads the prose, ordered so the columns that settle a question come first, capped, and silent where nothing was measured. 11 tests. | measured 2026-09-15 | **closed 2026-09-15** |
| B-13 | P1 | **A nightly orphaned by a deploy is put back now.** The sweep handled `index_repo` alone, on the recorded grounds that the nightly cron covers the short kinds — true of a hand-started `db_index`, false of the cron itself. Measured 2026-09-14: a release at 22:20 UTC SIGTERMed the worker 20 min into a nightly begun at 22:00; `index_repo` had already finished so the sweep found nothing, and the `daily_sync` parent was reaped and never retried. Fixed: all four kinds are closed (a row left `running` blocks its own replacement through the partial unique index), `daily_sync` is re-enqueued, and `db_index`/`code_db_sync` are deliberately not — they run synchronously inside `run_for_project`, so the parent redoes them and a child enqueue would collide. The test asserting the old behaviour was replaced, not extended. | production incident 2026-09-14 | **closed 2026-09-15** |
| B-14 | P1 | **The background model, chosen on measurement rather than tier.** With B-06's cap fixed, the current `deepseek/deepseek-v4-flash-0731` decides 13/14 — but calls 7 of those tables `db_only` when every one has an ORM entity in the code, so the verdict is decided and wrong. Eight candidates run over the same fourteen production tables, cost projected on the measured 7 890 analyses/month and each model's own measured verbosity: `qwen/qwen3.7-flash` $2.86 (14/14 decided, 9 matched); current flash $4.59 (14/14, 4 matched); **`deepseek/deepseek-v3.2` $9.05 (14/14, 13 matched, the most concise at 1456 output tokens)**; `minimax/minimax-m2.5` $16.28 (14/14, 13 matched, fastest at 22.8 s); `z-ai/glm-5.3-flash` $21.16 (11/14); `qwen/qwen3.7-plus` $25.75 (14/14, 6 matched); `z-ai/glm-4.7` $66.17 (6/14 — too verbose to finish); `deepseek/deepseek-v4-pro-0813` $96.88 (13/14, 8 matched). **The operator asked for V4 Pro everywhere; the measurement refuses it** — Pro scores below v3.2 on the same tables at 10.7x the price, so the recommendation is `deepseek/deepseek-v3.2` for `DEFAULT_LLM_MODEL`. One `heroku config:set`, reversible. | probe over 14 production tables, live OpenRouter catalogue, 2026-09-15 | open — operator's choice |
| B-07 | P3 | ~130 stale remote branches on the origin (`fix/*`, `docs/*` from closed work). Housekeeping; delete after confirming each is merged | PRJ-01 observation | open |
| B-05 | P3 | **Closed as a class.** The row named one stale comment in `orchestrator.py`; measured, **13 comments promised a test and 5 named none** — including the guard on `context.extra`, whose defect left `exposed_learning_ids` empty on every request ever served. All six named now (the sixth found by the guard, not the survey). `test_a_promised_test_is_named_and_exists.py` enforces two rules: a promise names a `test_*.py`, and the file exists. The name check runs over EVERY comment rather than only promise-phrased ones — otherwise naming a test removes the phrasing and the new reference stops being checked, leaving the guard green exactly where the work was done. Both rules verified against planted defects. | measured 2026-09-15 | **closed 2026-09-15** |

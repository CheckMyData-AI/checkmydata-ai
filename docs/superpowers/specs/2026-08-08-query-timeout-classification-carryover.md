# Carry-over ledger — query-timeout classification

> Append-only. Written by every stage the moment something is deferred, dropped or
> left half-done. Read in full at stage 10; counts printed beside every gate verdict.

| # | Stage | Item | Why | Where it goes | Status |
|---|---|---|---|---|---|
| K1 | 0 | **Process-wide circuit breaker per connection** (Redis-backed, half-open, cross-request cooldown) | D2 chose a per-run in-memory counter. A shared breaker needs multi-dyno state, half-open probing and tenant isolation — a distinct brief, and a stuck `open` would silence a healthy database | `BACKLOG.md` | deferred |
| K2 | 0 | **Code graph not built** — `graphify-out/` holds only a detect cache, no `graph.json`, so the harvest could not ask reach questions ("what else calls the classifier") | Graph is a recommendation, never a gate | build with `/graphify .` before the next run | open |
| K3 | 0 | **`docs/superpowers/retro.md` does not exist** — this project has run four pipelines with no standing instructions carried between them | Stage 10 creates it | stage 10 of this run | open |
| K4 | 0 | **Inherited from m0, still open: C13 — `ssh_key_id` has no ownership check** on connection create/update; `to_config` is called without `user_id` on chat/SSE/WS paths, so `SshKeyService.get_decrypted(…, user_id=None)` is an unfiltered lookup by id. A tenant who learns another tenant's SSH key id can attach it and have the server tunnel with someone else's private key | Pre-existing on `main`; the m0 ledger named it "the next piece of work" and it was not taken | `BACKLOG.md` — **security, recommended next** | **open — security** |
| K5 | 0 | Inherited from m0 and still open: C7 (`query_mcp_source` description still names Google Analytics), C14 (`ANALYTICS_COLLECT_ENABLED` read once, worker never checks it), C19 (9 unresolvable `Coverage:` paths in `docs/ux/scenarios.md`, ratcheted at ≤9) | Out of this brief's scope | `BACKLOG.md` | open |
| K6 | 0 | **`sentence-transformers` absent from the production image** — boot log shows the embedder silently falling back from `BAAI/bge-base-en-v1.5` (768-d) to `all-MiniLM-L6-v2` (384-d), and `reranker_enabled` degrading to a no-op. This is deploy note 3 in `CLAUDE.md`, never executed | Found during the log analysis that produced this brief; unrelated to the timeout defects | `BACKLOG.md` | open |
| K7 | 0 | **Worker OOM**: `Error R14 (Memory quota exceeded)`, `mem=843M (163.0%)` continuously from 03:07 to the 04:30 UTC restart on 2026-08-07 | Separate from this brief | `BACKLOG.md` | open |
| K8 | 0 | **`MCP_ALLOWED_HOSTS` empty in production** — DNS-rebinding Host validation disabled (F-MCP-04), warned at every boot | Separate from this brief | `BACKLOG.md` | open |
| K9 | 0 | **A connection is configured against `127.0.0.1`** — `MySQL test_connection failed: (2003, "Can't connect to MySQL server on '127.0.0.1'")` every 5 minutes before the 2026-08-07 04:44 restart, with no SSH-tunnel activity in the log window | **Re-diagnosed 2026-08-10 — the hypothesis was right and the framing was wrong.** The connection is `db-esim` (mysql, project `esim-php`, owner sergey@appvillis.com, created 2026-03-19) and it has `ssh_host = 64.188.10.62`. `127.0.0.1` is the **tunnel endpoint and is correct configuration** — there is no misconfigured host to fix. The defect is that the SSH tunnel does not re-establish, so the probe hits a dead local port | `BACKLOG.md` — retitled *SSH tunnel does not re-establish* | open |
| K10 | 2 | **`SQLAgent._wall_clock_remaining` is shared mutable state across concurrent requests** — `chat.py:60` makes `ConversationalAgent()` a module-level singleton, and the `OrchestratorAgent`/`SQLAgent` beneath it are built once in `__init__`, so up to `max_concurrent_agent_calls=3` requests overwrite each other's budget | Pre-existing on `main`, found while designing REQ-006 | **Fixed in this run** under D10 — the field becomes a parameter | in scope |
| K11 | 2 | Other per-request state on the singleton agent chain was **not** audited — only the field this change had to cross | Unrelated refactor is out of scope per the brainstorm doctrine | `BACKLOG.md` — audit `self.`-assignments on `SQLAgent`/`OrchestratorAgent` for the same shape | open |
| K12 | 5 | **REQ-007 partially met.** `failure_kind` now reaches the trace from both abnormal REST paths, but the **router signals (`route`, `complexity`) are still `"unknown"` on a trace written by the buffer flush at `pipeline_end`** — the path the production row of 2026-08-06 actually came from. Fix: `_persist_workflow` (`trace_persistence_service.py:370`) should read them from `_WorkflowBuffer.context` (`:124`), which the orchestrator would populate when the router resolves (`orchestrator.py:738-749` already writes them to `context.extra`). Separately, the REST path still cannot capture the real workflow id the way the SSE path does (`chat.py:1018-1019`), so its fallback row cannot be joined to its spans — now logged rather than silent | Needs a change inside the buffer/event path; scoped out of this run's remaining budget rather than rushed | — | **closed 2026-08-08.** Root cause was sharper than the ledger entry: `finalize_trace`'s UPDATE listed every column unconditionally, so its own parameter defaults erased the duration, steps and routing signals the buffer flush had already written. The UPDATE is now additive. REQ-007 met |
| K13 | 6 | **A test written after its implementation passed against a planted defect.** The first no-clobber check asserted on `finalize_trace`'s *source text*; a blanket-overwrite defect preserved the text it looked for and sailed through. Rewritten to intercept the emitted UPDATE and assert on the columns actually written | Found by the stage-6 probe, which is exactly what the probe exists for | **Retro candidate**: source-text assertions are not checks — a test written after the code must be probed before it is trusted | closed |
| K14 | 8 | **Behavioural proof in production is not available and is not claimed.** The timeout path can only be exercised by a real question against a database that accepts EXPLAIN and never returns rows. Production has had **no chat traffic at all** since the 2026-08-07 04:44 restart (verified: zero `api/chat` entries across the log window, and the `health_check` workflow — which only fires once a connector pool is registered — has not run since). So stage 8 proves a clean boot of the right commit, not the fixed behaviour | Cannot be manufactured without traffic or a deliberately stalled test database | **Closed 2026-08-10 by replacing the wish with a test.** "Verify on the next real timeout" was never a task — pre-launch there is no next real timeout, so the path would have shipped unverified indefinitely. `tests/integration/test_timeout_path_end_to_end.py` stands up a database with the 2026-08-06 shape (EXPLAIN answers instantly, execution never returns) and drives the composed path: **two executions, one repair, failure still typed TIMEOUT**, against ten repairs and 277 831 ms in production. A second case proves the one repair is spent rather than skipped, so a genuinely heavy query is still fixed. Probed by restoring the pre-fix budget: red | **closed** |

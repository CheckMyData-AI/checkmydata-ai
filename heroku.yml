build:
  docker:
    web: Dockerfile.backend
    worker: Dockerfile.backend
run:
  web: sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
  worker: sh -c "/opt/venv/bin/arq app.worker.WorkerSettings"
